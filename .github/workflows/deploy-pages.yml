name: SCRAP PLANET iPhone QA deployment gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment gate
        run: |
          echo 'The public QA site is already deployed.'
          echo 'A verified private build artifact and its SHA-256 values are injected only during an approved QA update.'
          echo 'This dormant workflow intentionally contains no expiring artifact URL and performs no deployment by itself.'
